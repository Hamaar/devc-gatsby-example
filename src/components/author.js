import React from "react"
import "./author.style.css"

const Profile = ({ name }) => (
  <section className="author">
    <div style={{

    }}>
      <img style={{

      }} />
    </div>
    <div>
      name
      bio
      button
    </div>
  </section>
)

export default Profile